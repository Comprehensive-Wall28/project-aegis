{
    "watch": [
        "src"
    ],
    "ext": "ts",
    "exec": "tsx --env-file=.env src/fastify-server.ts",
    "events": {
        "restart": "logger 'Backend restarted due to file changes'"
    }
}