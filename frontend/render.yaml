# Render Blueprint - SPA fallback for client-side routing
# Note: Render serves static files first, redirects only apply to non-existent paths
routes:
  - type: rewrite
    source: /*
    destination: /index.html

